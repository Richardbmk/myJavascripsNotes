you need to install a live server in order to work with fecth

You already have installed liver server, if not install 
npm live-server

in the terminal:
> liver-server


        d.acceleration = +d.acceleration,
        d.cylinders = +d.cylinders,
        d.displacement = +d.displacement,
        d.horsepower = +d.horsepower,
        d.yar = 1900 +d.year,
        d.mpg = +d.mpg,
        d.weight = +d.weight